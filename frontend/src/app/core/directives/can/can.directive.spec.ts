import { CanDirective } from './can.directive';

describe('CanDirective', () => {
  it('should create an instance', () => {
    const directive = new CanDirective();
    expect(directive).toBeTruthy();
  });
});
